import { Link } from "react-router-dom";

export default () => {

    return (
        <>
            <h1>About Page</h1>
            <Link to={`/`}>Home</Link>            
            
        </>
    )
}